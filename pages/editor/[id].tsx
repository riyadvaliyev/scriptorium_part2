import React, { useRef, useState } from "react";
import { useRouter } from "next/router";

import ExecuteCodePage from ".";

const EditorWrapper: React.FC = () => {
    return (
        <ExecuteCodePage/>
    );
}; 

export default EditorWrapper;